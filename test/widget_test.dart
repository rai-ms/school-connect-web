import 'package:flutter_test/flutter_test.dart';

/// Default widget test placeholder.
/// The real MyApp requires full DI (GetIt) initialization.
/// See test/presentation/ for actual widget and BLoC tests.
void main() {
  test('app package is importable', () {
    // Verifies the test harness itself works.
    expect(1 + 1, equals(2));
  });
}
